version: "3.7"
services:
  app:
    build:
      args:
        user: hubertzol
        uid: 1000
      context: ./
      dockerfile: Dockerfile
    image: laravel-react-app
    container_name: laravel-react-app
    restart: unless-stopped
    working_dir: /var/www/
    volumes:
      - ./:/var/www
    networks:
      - laravelnetwork

  db:
    image: mysql:5.7
    container_name: laravel-react-app-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    volumes:
      - ./docker-compose/mysql:/docker-entrypoint-initdb.d
    networks:
      - laravelnetwork

  nginx:
    image: nginx:alpine
    container_name: laravel-react-app-nginx
    restart: unless-stopped
    ports:
      - 8000:80
    volumes:
      - ./:/var/www
      - ./docker-compose/nginx:/etc/nginx/conf.d/
    networks:
      - laravelnetwork

networks:
  laravelnetwork:
    driver: bridge
